{
  "name": "celestial-patterns",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "npm run -s serve",
    "build": "if-env NODE_ENV=production && preact build",
    "serve": "node ./node_modules/preact-cli/lib/index.js build && node ./node_modules/preact-cli/lib/index.js serve",
    "dev": "preact watch",
    "preact-cli": "^1.4.1",
    "if-env": "^1.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.3.0",
    "eslint-config-synacor": "^1.0.1"
  },
  "dependencies": {
    "preact": "^8.2.1",
    "preact-compat": "^3.16.0",
    "preact-router": "^2.5.5",
    "react-d3-basic": "^1.6.11"
  }
}
