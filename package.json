{
  "name": "celestial-patterns",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && node server.js",
    "dev": "preact watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.3.0",
    "eslint-config-synacor": "^1.0.1"
  },
  "dependencies": {
    "express": "^4.15.3",
    "if-env": "^1.0.0",
    "preact": "^8.2.1",
    "preact-cli": "^1.4.1",
    "preact-compat": "^3.16.0",
    "preact-router": "^2.5.5",
    "react-d3-basic": "^1.6.11",
    "serve-favicon": "^2.4.3",
    "serve-static": "^1.12.4"
  }
}
